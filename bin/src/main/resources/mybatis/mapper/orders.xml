<?xml version="1.0" encoding="euc-kr" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
					 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
					 
<mapper namespace="com.mycompany.webapp.dao.OrdersDao">
	
	<select id="selectByPage" resultType="order">
		select rnum,userId,orderNo,allPrice,orderDate,orderState,zipCode,roadAddress,detailAddress
		from (
			select rownum as rnum,userId,orderNo,allPrice,orderDate,orderState,zipCode,roadAddress,detailAddress
			from (
				select 	
					user_id as userId,
					order_no as orderNo,
					all_price as allPrice, 
					order_date as orderDate, 
					order_state as orderState,
					zip_code as zipCode,
					road_address as roadAddress,
					detail_address as detailAddress
					from orders
					order by orderDate desc
			)
			where rownum &lt;= #{endRowNo}
		)
		where rnum &gt;= #{startRowNo}
  	</select>
	
	<select id = "count" resultType="int">
		select count(*) from orders	 
	</select>
	
	<select id="selectByOrderNo" parameterType="int" resultType="order">
		select
			user_id as userId,
			all_price as allPrice, 
			order_date as orderDate, 
			order_state as orderState,
			zip_code as zipCode,
			road_address as roadAddress,
			detail_address as detailAddress
		from orders
		where order_no=#{orderNo}
	</select>
	
	<!-- <insert id="insert" parameterType="order">
		insert into orders (order_no, user_id, order_date, all_price, order_state, zip_code, road_address, detail_address)
		values(seq_orders_order_no.nextval, #{userId}, #{orderDate}, #{allPrice}, 0, #{zipCode}, #{roadAddress}, #{detailAddress})
	</insert> -->
	
	<delete id="deleteByOrderNo" parameterType="int">
		delete from orders where order_no=#{orderNo}
	</delete>
	
	<!-- <update id="update" parameterType="int">
	  	update orders set
	  		order_state = 1
	  	where order_no = #{orderNo}
 	</update> -->
  
  <update id="update" parameterType="productsRefund">
  	update orders set
  		order_state = 1
  	where order_no = #{orderNo}
  </update>
  
</mapper>
